controller_manager:
  ros__parameters:
    update_rate: 100
    joint_state_broadcaster:
      type: joint_state_broadcaster/JointStateBroadcaster
    diff_drive_controller:
      type: diff_drive_controller/DiffDriveController

diff_drive_controller:
  ros__parameters:
    left_wheel_names: [left_wheel_joint]
    right_wheel_names: [right_wheel_joint]
    wheel_separation: 0.30
    wheel_radius: 0.075
    wheel_separation_multiplier: 1.0
    left_wheel_radius_multiplier: 1.0
    right_wheel_radius_multiplier: 1.0
    publish_rate: 50.0
    odom_frame_id: odom
    base_frame_id: base_footprint
    enable_odom_tf: true
    open_loop: false
    position_feedback: true
    use_stamped_vel: false
    cmd_vel_timeout: 0.5
    preserve_turning_radius: true
    publish_limited_velocity: true
    linear.x.has_velocity_limits: true
    linear.x.max_velocity: 0.60
    linear.x.min_velocity: -0.30
    linear.x.has_acceleration_limits: true
    linear.x.max_acceleration: 0.80
    linear.x.min_acceleration: -0.80
    angular.z.has_velocity_limits: true
    angular.z.max_velocity: 1.50
    angular.z.min_velocity: -1.50
    angular.z.has_acceleration_limits: true
    angular.z.max_acceleration: 2.00
    angular.z.min_acceleration: -2.00
